/**
 * Takes in 2 numbers, returns their sum
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */

const solution = (a, b) => {
  return a+b;
};
console.log(solution(2,3));
console.log(solution(5,5));

module.exports = {
  solution,
};
